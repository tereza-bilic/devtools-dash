// generates an array with all combinations of 3 letters from the alphabet
console.log('From all these combinations, you need to find the one which gives you the secret code: ');
console.log('it is created using potential secret B2');

setInterval(() => {
  //full secret here depends on a potentialSecret;
  console.log(fullSecret);
}, 20);
